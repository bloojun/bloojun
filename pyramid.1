#include <stdio.h>

void pyramid(int b)
{
	int i,x,g;
	int* pB = &b;

	for(i=1; i<=*pB; i++)
	{
		for(x=1;x<=(*pB-i); x++)
			printf("  ");
		for(g=0; g<2*i-1; g++)
			printf("■");
		printf("\n");
	}
}
	
int main(void)
{
	int a;
	int* pA = &a;
	
	while(1){
	printf("pyramid height (0 = exit) : ");
	
	scanf("%d", &a);
	if(a<1) 
		return 0;
	else if(a>=1)
		printf("\n");
		pyramid(*pA);
	printf("\n");
	}
	return 0;
}
